# Modelo de dominio

**Se genera desde los tipos.** **Generado desde los tipos. El generador todavía no existe**: hasta que lo haya, la fuente de verdad son los tipos de `packages/domain/src/entities/` y sus tests.

Este archivo se sobrescribe: no editarlo a mano. La fuente de verdad son los tipos de TypeScript y sus tests de persistencia.

Ver ADR-014 para el porqué de cada entidad.
