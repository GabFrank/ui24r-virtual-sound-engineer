# Modelo de dominio

**Se genera desde los tipos.** Ejecutar `npm run docs:domain` para regenerarlo desde `packages/domain/src`.

Este archivo se sobrescribe: no editarlo a mano. La fuente de verdad son los tipos de TypeScript y sus tests de persistencia.

Ver ADR-014 para el porqué de cada entidad.
